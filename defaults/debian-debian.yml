---
# file: grafana/defaults/debian-debian.yml
#
# Debian variables.
#

grafana_apt_dependencies:
  - apt-transport-https
  - software-properties-common

grafana_apt_distribution: "{{ ansible_distribution_release | lower }}"
