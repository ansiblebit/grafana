---
# file: primogen/tests/test.yml

- name: tests play
  hosts: "{{ vagrant_box }}"
  gather_facts: yes
  vars:
    debug: yes

  vars:
    debug: true

  roles:
    - role: primogen

    - role: tests
      tags: test

